<PowerSelectMultiple @placeholder="Berchem, Hasselt, Gent, ..." @search={{perform this.search}} @searchPlaceholder="Typ om te zoeken..." @options={{this.options}} @searchEnabled={{true}} @selected={{this.selected}} @onChange={{this.changeSelected}} @allowClear={{true}} as |bestuurseenheid|>
  {{bestuurseenheid.naam}} ({{bestuurseenheid.classificatie.label}})
</PowerSelectMultiple>
