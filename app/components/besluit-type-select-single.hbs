<PowerSelect
  @placeholder="Besluitenlijst, jaarrekening, ..."
  @search={{perform this.search}}
  @searchPlaceholder="Typ om te zoeken..."
  @searchEnabled={{true}}
  @options={{this.options}}
  @selected={{this.selected}}
  @onChange={{this.changeSelected}}
  @allowClear={{true}}

  as |besluitType|>
  {{besluitType.label}}
</PowerSelect>
