<PowerSelectMultiple @placeholder="Antwerpen, Oost-Vlaanderen, ..." @search={{perform search}} @searchPlaceholder="Typ om te zoeken..." @searchEnabled={{true}} @options={{options}} @selected={{selected}} @onChange={{action "changeSelected"}} @allowClear={{true}} as |werkingsgebied|>
  {{werkingsgebied.naam}}
</PowerSelectMultiple>
