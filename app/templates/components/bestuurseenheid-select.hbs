<PowerSelectMultiple @placeholder="Berchem, Hasselt, Gent, ..." @search={{perform search}} @searchPlaceholder="Typ om te zoeken..." @options={{options}} @searchEnabled={{true}} @selected={{selected}} @onChange={{action "changeSelected"}} @allowClear={{true}} as |bestuurseenheid|>
  {{bestuurseenheid.naam}} ({{bestuurseenheid.classificatie.label}})
</PowerSelectMultiple>
