<PowerSelectMultiple @placeholder="OCMW, Gemeente, ..." @search={{perform search}} @searchPlaceholder="Typ om te zoeken..." @searchEnabled={{true}} @options={{options}} @selected={{selected}} @onChange={{action "changeSelected"}} @allowClear={{true}} as |classificatie|>
  {{classificatie.label}}
</PowerSelectMultiple>
