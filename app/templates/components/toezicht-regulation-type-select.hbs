<PowerSelect @placeholder="Politieregelment, Personeelsregelment, ..." @search={{perform search}} @searchPlaceholder="Typ om te zoeken..." @options={{options}} @selected={{selected}} @onChange={{action "changeSelected"}} @allowClear={{true}} as |besluitType|>
  {{besluitType.label}}
</PowerSelect>
