<PowerSelectMultiple @placeholder="Besluitenlijst, jaarrekening, ..." @search={{perform search}} @searchPlaceholder="Typ om te zoeken..." @searchEnabled={{true}} @options={{options}} @selected={{selected}} @onChange={{action "changeSelected"}} @allowClear={{true}} as |besluitType|>
  {{besluitType.label}}
</PowerSelectMultiple>
