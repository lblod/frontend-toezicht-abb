import inzendingenFilter from 'frontend-toezicht-abb/utils/inzendingen-filter';
import { module, test } from 'qunit';

module('Unit | Utility | inzendingen-filter', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = inzendingenFilter();
    assert.ok(result);
  });
});
